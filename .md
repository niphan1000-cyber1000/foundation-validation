## 🛠️ Development Setup & Pre-commit Hook

To ensure code quality and prevent non-compliant commits, please install the local pre-commit hook after cloning the repository:

```bash
# Run the installation script (Linux/Mac/Git Bash)
sh scripts/install-hooks.sh